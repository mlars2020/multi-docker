sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mlars20/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run mlars20/react-test npm test -- --coverage

after_success:
  - docker build -t mlars20/multi-client ./client
  - docker build -t mlars20/multi-nginx ./nginx
  - docker build -t mlars20/multi-server ./server
  - docker build -t mlars20/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push mlars20/multi-client
  - docker push mlars20/multi-nginx
  - docker push mlars20/multi-server
  - docker push mlars20/multi-worker
